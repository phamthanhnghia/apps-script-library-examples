// This script logs variable values during execution
function logVariables() {
  var name = 'John Doe';
  var age = 30;
  Logger.log('Name: %s, Age: %s', name, age); // Logs variables in a formatted string
}
